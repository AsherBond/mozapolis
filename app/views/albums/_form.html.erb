
<%= simple_form_for @album, :html => { :class => 'form-vertical' } do |f| %>
  <%= f.input :title %>
  <%= f.input :year %>
  <%= f.input :label %>
  <%= f.input :description, :input_html => { :class => "span4", :rows => 5 } %>
  <hr>
  <%= f.input :artwork %>

  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                albums_path, :class => 'btn', :data => { "loading-text" => "hello"} %>
  </div>
<% end %>
