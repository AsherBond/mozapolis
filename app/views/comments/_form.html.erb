<%= simple_form_for [@commentable, @comment] do |f| %>
  	<%= f.error_notification %>
    <%= f.input :content, :label => false, :as => :text, :input_html => {:class => "span4", :rows => 4}  %>
	<%= f.submit "Submit Comment", :class => "btn btn-primary" %>
<% end %>